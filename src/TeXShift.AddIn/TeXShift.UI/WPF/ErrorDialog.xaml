<Window x:Class="TeXShift.AddIn.UI.WPF.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding Title}"
        Width="560" Height="420"
        MinWidth="520" MinHeight="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Icon="/TeXShift.AddIn;component/Resources/TeXShift.ico"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="Microsoft YaHei UI, Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="Purple"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon Kind="Alert" Width="36" Height="36" Foreground="#D32F2F" Margin="0,0,12,0"/>
                <TextBlock Grid.Column="1" Text="{Binding UserMessage}" TextWrapping="Wrap" FontSize="14"/>
            </Grid>
        </materialDesign:Card>

        <!-- Technical Details Section -->
        <StackPanel Grid.Row="1">
            <ToggleButton x:Name="DetailsToggle" HorizontalAlignment="Left" Cursor="Hand"
                          Style="{StaticResource MaterialDesignFlatButton}" Padding="4">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon x:Name="ExpandIcon" Kind="ChevronRight" Width="20" Height="20"
                                             VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <materialDesign:PackIcon.RenderTransform>
                            <RotateTransform x:Name="IconRotation" Angle="0"/>
                        </materialDesign:PackIcon.RenderTransform>
                    </materialDesign:PackIcon>
                    <TextBlock Text="{Binding TechnicalDetailsHeader}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                </StackPanel>
            </ToggleButton>
            <Border x:Name="DetailsPanel" Margin="0,8,0,0" BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="4"
                    MaxHeight="0" Opacity="0" ClipToBounds="True">
                <TextBox Text="{Binding TechnicalDetails, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12" Padding="8" BorderThickness="0" MinHeight="150"/>
            </Border>
            <StackPanel.Triggers>
                <EventTrigger RoutedEvent="ToggleButton.Checked" SourceName="DetailsToggle">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="DetailsPanel" Storyboard.TargetProperty="MaxHeight"
                                             To="200" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetName="DetailsPanel" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetName="IconRotation" Storyboard.TargetProperty="Angle"
                                             To="90" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="ToggleButton.Unchecked" SourceName="DetailsToggle">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="DetailsPanel" Storyboard.TargetProperty="MaxHeight"
                                             To="0" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetName="DetailsPanel" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetName="IconRotation" Storyboard.TargetProperty="Angle"
                                             To="0" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </StackPanel.Triggers>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{Binding CopyButtonText}" Command="{Binding CopyCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0"/>
            <Button Content="{Binding OpenFolderButtonText}" Command="{Binding OpenFolderCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0"/>
            <Button Content="{Binding OkButtonText}" IsDefault="True" IsCancel="True" Click="OkButton_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
